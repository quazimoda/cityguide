'use client';import type React from 'react';import { useState } from 'react';const ok=(e:string)=>/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(e);
export function ContactForm(){const [state,setState]=useState({name:'',email:'',message:''});const [msg,setMsg]=useState('');function submit(e:React.FormEvent){e.preventDefault();if(!state.name||!state.email||!state.message)return setMsg('Please complete all required fields.');if(!ok(state.email))return setMsg('Please enter a valid email address.');// TODO: Add email provider, database, or CRM integration for contact messages here.
setMsg('Message received! This MVP does not send email yet.');setState({name:'',email:'',message:''})}return <form onSubmit={submit} className="space-y-4 rounded-3xl bg-white p-6 shadow-sm"><input className="w-full rounded-xl border p-3" placeholder="Name" value={state.name} onChange={e=>setState({...state,name:e.target.value})}/><input className="w-full rounded-xl border p-3" placeholder="Email" value={state.email} onChange={e=>setState({...state,email:e.target.value})}/><textarea className="min-h-32 w-full rounded-xl border p-3" placeholder="Message" value={state.message} onChange={e=>setState({...state,message:e.target.value})}/><button className="rounded-xl bg-teal-700 px-5 py-3 font-bold text-white">Send message</button>{msg&&<p className="text-sm text-teal-800">{msg}</p>}</form>}
