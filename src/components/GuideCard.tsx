import Link from 'next/link';import type { Article } from '@/types/content';import { categorySlugByTitle } from '@/lib/content';import { TagList } from './TagList';
export function GuideCard({guide}:{guide:Article}){return <article className="rounded-3xl bg-white p-6 shadow-sm ring-1 ring-orange-100"><p className="text-sm font-bold text-teal-700">{guide.category}</p><h3 className="mt-2 text-xl font-black"><Link href={`/guides/${categorySlugByTitle(guide.category)}/${guide.slug}`}>{guide.title}</Link></h3><p className="my-4 text-gray-600">{guide.excerpt}</p><TagList tags={guide.tags}/></article>}
