'use client';import type React from 'react';import { useState } from 'react';
const valid=(e:string)=>/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(e);
export function SubscribeForm(){const [email,setEmail]=useState('');const [msg,setMsg]=useState('');function submit(e:React.FormEvent){e.preventDefault();if(!email)return setMsg('Email is required.');if(!valid(email))return setMsg('Please enter a valid email.');// TODO: Connect future email provider, database, or CRM integration here.
setMsg('Thanks for subscribing! We will keep you posted.');setEmail('')}return <form onSubmit={submit} className="rounded-3xl bg-teal-900 p-6 text-white"><p className="mb-4">Enter your e-mail to receive Istanbul tips, travel recommendations, and exclusive local guides.</p><div className="flex flex-col gap-3 sm:flex-row"><input className="min-w-0 flex-1 rounded-xl px-4 py-3 text-gray-900" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com"/><button className="rounded-xl bg-orange-500 px-5 py-3 font-bold">Subscribe</button></div>{msg&&<p className="mt-3 text-sm">{msg}</p>}</form>}
