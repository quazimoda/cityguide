import { Hero } from '@/components/Hero';import { Container } from '@/components/Container';import { SectionHeader } from '@/components/SectionHeader';import { CategoryCard } from '@/components/CategoryCard';import { GuideCard } from '@/components/GuideCard';import { BlogCard } from '@/components/BlogCard';import { RecommendationCard } from '@/components/RecommendationCard';import { SubscribeForm } from '@/components/SubscribeForm';import { categories } from '@/data/categories';import { guides } from '@/data/guides';import { blogPosts } from '@/data/blog';import { recommendations } from '@/data/recommendations';
export default function Home(){return <><Hero title="Your Smart Guide to Living, Studying, and Exploring Istanbul" subtitle="Discover the best of Istanbul with expert local recommendations, hidden gems, travel tips, and practical guides."/><Container className="py-12"><SectionHeader eyebrow="Explore" title="City guide categories" description="Start with curated categories designed for tourists, international students, and expats."/><div className="grid gap-5 md:grid-cols-3">{categories.map(c=><CategoryCard key={c.slug} category={c}/>)}</div></Container><Container className="py-8"><SectionHeader title="Featured guides" description="Helpful planning articles for confident Istanbul days."/><div className="grid gap-5 md:grid-cols-3">{guides.slice(0,3).map(g=><GuideCard key={g.slug} guide={g}/>)}</div></Container><Container className="py-8"><SectionHeader title="Latest blog posts" description="Fresh practical ideas for discovering the city."/><div className="grid gap-5 md:grid-cols-3">{blogPosts.slice(0,3).map(p=><BlogCard key={p.slug} post={p}/>)}</div></Container><Container className="py-8"><SectionHeader title="Featured recommendations" description="Quick local picks for memorable moments."/><div className="grid gap-5 md:grid-cols-4">{recommendations.slice(0,4).map(r=><RecommendationCard key={r.slug} recommendation={r}/>)}</div></Container><Container><SubscribeForm/></Container></>}
