import type { Metadata } from 'next';import { Hero } from '@/components/Hero';import { Container } from '@/components/Container';import { SearchClient } from '@/components/SearchClient';export const metadata:Metadata={title:'Search',description:'Search Istanbul guides, blog posts, and recommendations.'};export default function Search(){return <><Hero title="Search Istanbul advice" subtitle="Find guides, blog posts, and local recommendations by topic, category, or content type."/><Container className="py-12"><SearchClient/></Container></>}
