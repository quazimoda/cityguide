import type { Metadata } from 'next';import { Hero } from '@/components/Hero';import { Container } from '@/components/Container';import { BlogCard } from '@/components/BlogCard';import { SubscribeForm } from '@/components/SubscribeForm';import { AdPlaceholder } from '@/components/AdPlaceholder';import { experienceOffers } from '@/data/offers';import { blogPosts } from '@/data/blog';export const metadata:Metadata={title:'Blog',description:'Istanbul blog posts with travel tips and local advice.'};export default function Blog(){return <><Hero title="Istanbul Blog" subtitle="Travel tips, student-friendly advice, hidden gems, and seasonal ideas."/><Container className="grid gap-5 py-12 md:grid-cols-2 lg:grid-cols-3">{blogPosts.map(p=><BlogCard key={p.slug} post={p}/>)}</Container><Container className="py-4"><AdPlaceholder offer={experienceOffers.neighborhoodWalk} sourceLabel="Blog listing partner placement" /></Container><Container><SubscribeForm/></Container></>}
